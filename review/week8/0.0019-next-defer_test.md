# Code Review: 0.0019/next/defer_test.go

**Rating: 3/10**

## Overview

Test file with one test function, commented-out function call, and no assertions. Tests defer behavior without verifying anything programmatically.

## What This Code Does

Single test that calls d1() from defer.go. Has c4() commented out. 9 lines with zero verification.

## Strengths

1. **Correct package name** - `playingWithDefer` matches defer.go
2. **Proper test signature** - `func TestDefer(t *testing.T)` is correct
3. **File exists** - At least attempted to write tests for educational code

## Issues

### Critical

1. **Zero assertions** - No `t.Fatal()`, no `t.Error()`, no verification of behavior
2. **No stdout capture** - Tests defer output but doesn't verify what prints or in what order
3. **Test doesn't test** - Just calls d1() and hopes human reads output. This is not a test.
4. **Commented code** - c4() is commented with no explanation why

### Major

1. **Tests only 1 of 7 functions** - defer.go has c1, c2, c3, c4, d1, but only d1 tested
2. **No verification of stack behavior** - Claims to understand LIFO but doesn't prove it in tests
3. **Missing table-driven tests** - Perfect use case for testing different defer scenarios
4. **No documentation** - No comment explaining what behavior is being verified

### Minor

1. **Misleading test name** - `TestDefer` is too generic. Should be `TestDeferStackOrderD1` or similar
2. **No test for c3==c4 bug** - defer.go has identical c3/c4 functions, test doesn't catch this
3. **Could use testify** - For complex output verification, assertion library would help

## What You Learned

- Test files need \_test.go suffix (correctly done)
- Tests need testing.T parameter (correctly done)

Did not learn:

- Tests must verify behavior, not just run code
- Table-driven tests for multiple scenarios
- How to capture and verify stdout in Go tests
- Assertions make tests readable

## Testing

This file IS a test file, but it doesn't test anything. It's a renamed main function that prints output for manual inspection.

## Final Verdict

**This is not a test. This is a runner.** Week 7 had test file with zero assertions (SingallyLinkedListtest2_test.go rated 3/10). Week 8 repeats exact same mistake. The pattern: Creating test files without understanding what tests do.

A real test for defer would:

```go
func TestDeferStackOrder(t *testing.T) {
    var output []string
    // Mock fmt.Println to capture output
    // Call d1()
    // Verify output appears in LIFO order
    if output[0] != "Exiting D1" {
        t.Fatal("First defer should print last")
    }
}
```

For educational code explaining defer, having tests that prove the behavior programmatically would be valuable. Instead, this is the second test file in two weeks with zero assertions.

**Fix: Add actual assertions or delete file.**

---

**Previous issues from other weeks:** Week 7 test2_test.go: 3/10, zero assertions. Week 8 defer_test.go: 3/10, zero assertions. Exact same problem repeated.
